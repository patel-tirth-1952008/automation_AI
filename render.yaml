services:
  - type: web
    name: bharatassist-ai

    runtime: python

    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium

    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT